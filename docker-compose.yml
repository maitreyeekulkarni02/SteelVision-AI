version: "3.9"

services:

  steelvision-ai:

    build: .

    container_name: steelvision-ai

    ports:
      - "8501:8501"

    restart: unless-stopped

    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434